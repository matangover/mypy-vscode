{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run extensions",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                // "--disable-extensions",
                "--extensionDevelopmentPath=${workspaceFolder}"
            ],
            "preLaunchTask": "Build extension"
        }
    ]
}