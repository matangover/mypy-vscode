{
    "name": "mypy",
    "description": "Type checking and rich language features for Python using mypy.",
    "author": "Matan Gover",
    "repository": "https://github.com/matangover/mypy-language-server",
    "license": "MIT",
    "version": "0.0.1",
    "publisher": "matangover",
    "engines": {
        "vscode": "^1.15.0"
    },
    "categories": [
        "Programming Languages",
        "Linters",
        "Other"
    ],
    "activationEvents": [
        "onLanguage:python",
        "workspaceContains:**/*.py"
    ],
    "contributes": {
        "configuration": {
            "title": "Mypy extension configuration",
            "type": "object",
            "properties": {
                "mypy.executable": {
                    "type": ["string", "null"],
                    "default": null,
                    "description": "Mypy language server executable"
                },
                "mypy.configFile": {
                    "type": ["string", "null"],
                    "default": null,
                    "description": "Mypy config file, relative to the workspace folder. Set to null (the default) to search in the default locations."
                },
                "mypy.targets": {
                    "type": "array",
                    "default": ["."],
                    "items": {
                        "type": "string"
                    },
                    "uniqueItems": true,
                    "description": "List of paths to type check, relative to the workspace folder. The default is to check the workspace folder."
                }
            }
        }
    },
    "main": "./out/extension",
    "scripts": {
        "vscode:prepublish": "tsc -p ./",
        "compile": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "vscode": "npm run vscode:prepublish && VSCODE=$(which code-insiders || which code || echo echo ERROR: neither the code nor code-insiders vscode executable is installed); USER=dummy-dont-share-vscode-instance $VSCODE --user-data-dir=$PWD/.vscode-dev/user-data --extensionHomePath=$PWD/.vscode-dev/extensions --extensionDevelopmentPath=$PWD $*"
    },
    "devDependencies": {
        "typescript": "^2.3.4",
        "vscode": "^1.1.4",
        "mocha": "^2.3.3",
        "@types/node": "^6.0.40",
        "@types/mocha": "^2.2.32"
    },
    "dependencies": {
        "vscode-languageclient": "^3.4.5"
    }
}
